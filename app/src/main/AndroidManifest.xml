<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissão para acessar a internet -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permissão para verificar o estado da rede -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Permissão para acessp ao aarmazenamento externo -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACESS_WIFI_STATE"/>



    <application
        android:name=".session.AppSession"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/key_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/key_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Game"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">


        <!--Tela de Splash-->
        <activity
            android:name=".ui.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.CadastroActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true"
            android:screenOrientation="unspecified"
            tools:ignore="DiscouragedApi" /> <!-- "unspecified" evita da Activity se reaplicar na tela com a mudança para o modo paisagem -->
        <activity
            android:name=".ui.activities.AdminActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>
