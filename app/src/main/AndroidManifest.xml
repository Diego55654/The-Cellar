<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--Permissão para acessar a internet-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--Permissão para verificar o estado da rede-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!--Permissão para acessp ao aarmazenamento externo-->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:name=".session.AppSession"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Game"
        tools:targetApi="31">

        <activity
            android:name=".ui.activities.CadastroActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="unspecified"
            tools:ignore="DiscouragedApi" />
        <!--"unspecified" evita da Activity se reaplicar na tela com a mudança para o modo paisagem-->
        <activity
            android:name=".ui.activities.AdminActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>